# trkit proposal embed configuration.
# Public key is safe to expose to the browser via the SDK script tag.
# Legacy TRIPKIT_* aliases are also accepted by the API route.
TRKIT_PUBLIC_KEY=pk_live_XXXX

# Secret key is server-only and used by /api/trkit-register.
TRKIT_SECRET_KEY=sk_live_XXXX

